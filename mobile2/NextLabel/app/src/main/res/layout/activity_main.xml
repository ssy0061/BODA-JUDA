<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:background="@color/brand_color">

    <ImageView
        android:id="@+id/img_sample_logo"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:src="@drawable/sample_logo"
        android:contentDescription="logo image"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="25dp"
        android:layout_marginStart="25dp" />

    <TextView
        android:id="@+id/txt_service_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="NextLabel"
        android:textSize="22sp"
        android:textColor="@color/black"
        app:layout_constraintTop_toTopOf="@id/img_sample_logo"
        app:layout_constraintBottom_toBottomOf="@id/img_sample_logo"
        app:layout_constraintStart_toEndOf="@id/img_sample_logo"
        android:layout_marginStart="10dp" />

    <ImageView
        android:id="@+id/btn_notification"
        android:layout_width="33dp"
        android:layout_height="33dp"
        android:src="@drawable/notification"
        android:contentDescription="notification button"
        app:layout_constraintTop_toTopOf="@id/img_sample_logo"
        app:layout_constraintBottom_toBottomOf="@id/img_sample_logo"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginEnd="25dp" />

    <FrameLayout
        android:id="@+id/area_fragment"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:background="@drawable/round_corner_top"
        app:layout_constraintTop_toBottomOf="@id/img_sample_logo"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="25dp" />

</androidx.constraintlayout.widget.ConstraintLayout>